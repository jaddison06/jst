types:
  Person:
    id: int
    name: string
    boring: bool

objects:
  people: list[Person]

actions:
  addPerson:
    params:
      person: Person
  removePerson:
    params:
      id: int
    returns: bool